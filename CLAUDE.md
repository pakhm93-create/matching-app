@AGENTS.md

# 성향 기반 연애 매칭 앱 (가칭)

## 이 문서는 무엇인가
Claude Code가 세션마다 자동으로 읽는 프로젝트 설명서입니다.
대화 기록이 끊기거나 계정이 바뀌어도 이 파일만 있으면 맥락이 이어집니다.
**중요한 결정을 내릴 때마다 여기에 기록합니다.**

## 프로젝트 개요
아주 상세한 설문(종교, 정치, 음주, 지역, 나이, 취향, 성격 등)을 진행시킨 뒤,
응답을 데이터화해 **성향이 매우 비슷한 상대를 매칭**해주는 앱.
단순 추천이 아니라 **주기적 배급(5일장 모델)**으로 희소성을 만들고,
그 지점에서 **매칭 토큰 결제**를 유도한다.

- **대상**: 일반 사용자 전체
- **형태**: 모바일 웹앱 (휴대폰 브라우저 우선, PC도 동작). 앱스토어 출시 아님
- **현재 목표**: 실서비스가 아니라 **작동하는 시제품(MVP)** 먼저

## 개발자 배경 (중요)
프로젝트 소유자는 **개발 경험이 전혀 없음**.
- 코드를 직접 수정하지 않음. 모든 구현은 Claude가 담당
- 전문 용어는 반드시 풀어서 설명할 것
- 터미널 명령어는 복사해 붙여넣을 수 있는 형태로, 왜 실행하는지와 함께 제시
- 선택지를 던지기보다 **추천안을 제시하고 이유를 설명**할 것

## 기술 스택
| 항목 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js (App Router) + TypeScript | 모바일 웹 표준, Vercel 무료 배포 |
| 스타일 | Tailwind CSS | 모바일 반응형을 빠르게 |
| DB/인증 | Supabase (2단계에서 도입) | 무료 티어, 로그인 내장 |
| 배포 | Vercel | 무료, git 연결 시 자동 배포 |

> ⚠️ 이 Next.js 버전은 학습 데이터와 API가 다를 수 있음.
> 코드 작성 전 `node_modules/next/dist/docs/` 의 해당 가이드를 반드시 읽을 것. (AGENTS.md 참조)

## 확정된 핵심 설계 결정

1. **우선순위 TOP 3** — 문항마다 중요도를 묻지 않는다. 설문 시작 전 한 화면에서
   "절대 양보 못 하는 조건 3개"만 고르게 한다. 이 3개가 하드 필터 + 가중치 5배 역할
2. **하드 필터 우선** — 딜브레이커는 점수에 섞지 않고 후보에서 아예 제외
3. **양방향 기하평균** — 한쪽만 만족하는 매칭을 걸러냄
4. **성격 측정은 Big Five 기반, 단 사용자에게 노출하지 않는다** — 화면에
   "성격 검사", "Big Five" 같은 표현을 쓰지 않고 일반 문항 사이에 섞어 배치
5. **배급 모델** — 상위 N명을 한 번에 보여주지 않고 주 1회 알림으로 내보냄
6. **과금은 속도와 양에만** — 돈을 내면 매칭이 "더 정확해지는" 구조는 만들지 않는다.
   정확도에 과금하면 매칭 품질 신뢰가 무너진다

## 개발 로드맵
### 1단계 — 코어 검증 (현재)  ※ 외부 계정 가입 불필요
우선순위 TOP 3 화면 + 설문 30문항 + 매칭 알고리즘.
**가짜 사용자 50명**으로 매칭이 말이 되는지 눈으로 확인. 로그인·DB 없음
> 목적: "이 매칭이 실제로 납득 가능한가"를 가장 빨리 확인

### 2단계 — 진짜 데이터
Supabase 연결. 회원가입/로그인, 설문 응답 저장, 실제 사용자 간 매칭

### 3단계 — 배급 + 연결
주기적 매칭 배급, 푸시 알림, 좋아요 → 매칭 성사 → 채팅

### 4단계 — 수익화
매칭 토큰 구매/소비, 결제 연동

### 5단계 — 실서비스 준비
개인정보 처리방침, 민감정보 별도 동의, 신고/차단, 본인인증, 보안 점검

### 이후 아이디어
- **데이트 코스 추천** — 매칭된 두 사람의 취향 데이터를 이미 갖고 있으므로
  "어디서 만나 무엇을 하면 좋을지" 추천. 이 앱만의 차별점이 될 수 있음

## 반드시 지켜야 할 제약
- **민감정보**: 종교·정치성향·성적지향은 한국 개인정보보호법상 **민감정보**로
  분리된 별도 동의 필요. MVP 이후 정식 구현하되 설계에 미리 반영
- **매칭 대상**: 초기 요구사항은 이성 매칭이나, 데이터 모델은 성별/지향을
  유연하게 담도록 설계해 추후 변경 시 재작업이 없게 할 것
- 개인정보를 URL 파라미터에 절대 넣지 않을 것

## 관련 문서
- `docs/survey-design.md` — 설문 문항 설계 (68문항, MVP는 30문항)
- `docs/matching-algorithm.md` — 매칭 계산 + 배급 + 수익화 설계

## 진행 상황
- [x] git 저장소 초기화 (main 브랜치)
- [x] 설문 문항 설계
- [x] 매칭 알고리즘 · 배급 · 수익화 설계
- [x] Node.js 설치 (v24.20.0)
- [x] Next.js 프로젝트 생성
- [x] 문항 데이터를 코드로 정의 (MVP 30문항)
- [x] 매칭 알고리즘 구현 + 가짜 데이터 50명으로 검증 (`npm run verify`)
- [ ] 우선순위 TOP 3 화면
- [ ] 설문 화면
- [ ] 매칭 결과 화면

## 검증 도구
```
npm run verify
```
가짜 사용자 50명 + 극단 케이스(거의 동일한 쌍 / 정반대 쌍)로 매칭 엔진을 점검한다.
알고리즘이나 가중치를 건드린 뒤에는 반드시 이 명령을 다시 돌려 결과를 확인할 것.

## 알아두어야 할 것 (검증에서 드러난 사실)
- **원점수는 그대로 쓰면 안 된다.** 5점 척도 특성상 아무 상관 없는 두 사람도
  원점수 0.5 근처가 나온다. `RAW_BASELINE`(현재 0.42)으로 눈금을 다시 그려
  사용자에게 보여준다. **실사용자 데이터가 쌓이면 이 값을 반드시 재조정할 것**
- **가짜 데이터는 완전 무작위**라 진짜로 궁합 높은 상대가 애초에 없다.
  그래서 최고 점수가 44점대로 낮게 나오는 것이 정상이다.
  실제 사용자는 성향이 뭉치므로 훨씬 높은 점수가 나온다.
  더 현실적인 검증을 하려면 "성향 클러스터 + 잡음" 방식으로 가짜 데이터를 개선할 것
