@AGENTS.md

# 성향 기반 연애 매칭 앱 (가칭)

## 이 문서는 무엇인가
Claude Code가 세션마다 자동으로 읽는 프로젝트 설명서입니다.
대화 기록이 끊기거나 계정이 바뀌어도 이 파일만 있으면 맥락이 이어집니다.
**중요한 결정을 내릴 때마다 여기에 기록합니다.**

## 프로젝트 개요
아주 상세한 설문(종교, 정치, 음주, 지역, 나이, 취향, 성격 등)을 진행시킨 뒤,
응답을 데이터화해 **성향이 매우 비슷한 상대를 매칭**해주는 앱.
단순 추천이 아니라 **주기적 배급(5일장 모델)**으로 희소성을 만들고,
그 지점에서 **매칭 토큰 결제**를 유도한다.

- **대상**: 일반 사용자 전체
- **형태**: 모바일 웹앱 (휴대폰 브라우저 우선, PC도 동작). 앱스토어 출시 아님
- **현재 목표**: 실서비스가 아니라 **작동하는 시제품(MVP)** 먼저

## 개발자 배경 (중요)
프로젝트 소유자는 **개발 경험이 전혀 없음**.
- 코드를 직접 수정하지 않음. 모든 구현은 Claude가 담당
- 전문 용어는 반드시 풀어서 설명할 것
- 터미널 명령어는 복사해 붙여넣을 수 있는 형태로, 왜 실행하는지와 함께 제시
- 선택지를 던지기보다 **추천안을 제시하고 이유를 설명**할 것

## 기술 스택
| 항목 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js (App Router) + TypeScript | 모바일 웹 표준, Vercel 무료 배포 |
| 스타일 | Tailwind CSS | 모바일 반응형을 빠르게 |
| DB/인증 | Supabase (2단계에서 도입) | 무료 티어, 로그인 내장 |
| 배포 | Vercel | 무료, git 연결 시 자동 배포 |

> ⚠️ 이 Next.js 버전은 학습 데이터와 API가 다를 수 있음.
> 코드 작성 전 `node_modules/next/dist/docs/` 의 해당 가이드를 반드시 읽을 것. (AGENTS.md 참조)

## 확정된 핵심 설계 결정

1. **우선순위 TOP 3** — 문항마다 중요도를 묻지 않는다. 설문 시작 전 한 화면에서
   "절대 양보 못 하는 조건 3개"만 고르게 한다. 이 3개가 하드 필터 + 가중치 5배 역할
2. **하드 필터 우선** — 딜브레이커는 점수에 섞지 않고 후보에서 아예 제외
3. **양방향 기하평균** — 한쪽만 만족하는 매칭을 걸러냄
4. **성격 측정은 Big Five 기반, 단 사용자에게 노출하지 않는다** — 화면에
   "성격 검사", "Big Five" 같은 표현을 쓰지 않고 일반 문항 사이에 섞어 배치
5. **배급 모델** — 상위 N명을 한 번에 보여주지 않고 주 1회 알림으로 내보냄
6. **과금은 속도와 양에만** — 돈을 내면 매칭이 "더 정확해지는" 구조는 만들지 않는다.
   정확도에 과금하면 매칭 품질 신뢰가 무너진다

## 개발 로드맵
### 1단계 — 코어 검증 (현재)  ※ 외부 계정 가입 불필요
우선순위 TOP 3 화면 + 설문 30문항 + 매칭 알고리즘.
**가짜 사용자 50명**으로 매칭이 말이 되는지 눈으로 확인. 로그인·DB 없음
> 목적: "이 매칭이 실제로 납득 가능한가"를 가장 빨리 확인

### 2단계 — 진짜 데이터
Supabase 연결. 회원가입/로그인, 설문 응답 저장, 실제 사용자 간 매칭

### 3단계 — 배급 + 연결
주기적 매칭 배급, 푸시 알림, 좋아요 → 매칭 성사 → 채팅

### 4단계 — 수익화
매칭 토큰 구매/소비, 결제 연동

### 5단계 — 실서비스 준비
개인정보 처리방침, 민감정보 별도 동의, 신고/차단, 본인인증, 보안 점검

### 이후 아이디어
- **데이트 코스 추천** — 매칭된 두 사람의 취향 데이터를 이미 갖고 있으므로
  "어디서 만나 무엇을 하면 좋을지" 추천. 이 앱만의 차별점이 될 수 있음

## 반드시 지켜야 할 제약
- **민감정보**: 종교·정치성향·성적지향은 한국 개인정보보호법상 **민감정보**로
  분리된 별도 동의 필요. MVP 이후 정식 구현하되 설계에 미리 반영
- **매칭 대상**: 초기 요구사항은 이성 매칭이나, 데이터 모델은 성별/지향을
  유연하게 담도록 설계해 추후 변경 시 재작업이 없게 할 것
- 개인정보를 URL 파라미터에 절대 넣지 않을 것

## 관련 문서
- `docs/survey-design.md` — 초기 설계안 (낡음). 현재 문항은 `src/lib/questions.ts` 96문항
- `docs/matching-algorithm.md` — 매칭 계산 + 배급 + 수익화 설계

## 진행 상황
- [x] git 저장소 초기화 (main 브랜치)
- [x] 설문 문항 설계
- [x] 매칭 알고리즘 · 배급 · 수익화 설계
- [x] Node.js 설치 (v24.20.0)
- [x] Next.js 프로젝트 생성
- [x] 문항 데이터를 코드로 정의 (MVP 30문항)
- [x] 매칭 알고리즘 구현 + 가짜 데이터 50명으로 검증 (`npm run verify`)
- [x] 우선순위 TOP 3 화면
- [x] 설문 화면 (한 화면 5문항, 임시 저장·이어서 하기)
- [x] 매칭 결과 화면 (성향 유형 + 궁합)
- [x] 피드백 반영: 선언형 절대조건, 활동지역, 출생연월, 8가지 성향 유형, 프로필 페이지
- [x] 기본정보를 객관적 정보만으로 축소, 나머지는 설문에서 파악
- [x] 휠 선택기 · 범위 슬라이더 · 한 화면 5문항 · 입력 오류 안내
- [x] 문항 96개로 확대 (축당 3문항 원칙, 영역별 균형, 가벼운 것부터 순서 배치)
- [x] 거리 기반 매칭 (권역 좌표 + 예상 이동 시간)
- [x] 애착 유형 궁합표 계산 (src/lib/attachment.ts)
- [x] Vercel 배포 — https://matching-app-roan.vercel.app (저장소 pakhm93-create/matching-app, 비공개)
- [x] Supabase 코드 준비 (스키마 · 접근 권한 · 메일 링크 로그인 · 저장/불러오기)
- [ ] Supabase 계정 만들고 연결 정보 넣기 — `docs/supabase-setup.md` 5단계

## 검증 도구
```
npm run verify
```
극단 케이스(거의 동일한 쌍 / 정반대 쌍), 하드 필터, 거리 계산, 매칭 기준별 후보 수,
영역별 비중, 화면 표시값을 한 번에 점검한다.
**문항이나 가중치를 건드린 뒤에는 반드시 이 명령을 다시 돌릴 것.**
특히 문항을 바꾸면 점수 분포가 이동하므로 `RAW_ANCHORS`를 다시 측정해야 한다.

## 알아두어야 할 것 (검증에서 드러난 사실)
- **원점수는 그대로 쓰면 안 된다.** 5점 척도 특성상 아무 상관 없는 두 사람도
  원점수 0.5 근처가 나온다. 그래서 백분위 기반으로 눈금을 다시 그린다
  (matching.ts의 `RAW_ANCHORS` / `SCORE_ANCHORS`). 아래 "매칭 기준" 항목 참고
- **가짜 사용자는 무작위로 만들면 안 된다.** 완전 무작위면 궁합 높은 상대가
  애초에 존재하지 않아 최고 점수가 40점대에 머물고, 기준 검증이 불가능해진다.
  지금은 성향 무리 6개 + 잡음 방식으로 만든다 (fake-users.ts)

## 앱 실행 방법
```
npm run dev
```
브라우저에서 http://localhost:3000 접속. 휴대폰에서 보려면 같은 와이파이에 연결한 뒤
터미널에 표시되는 `Network:` 주소로 접속한다.
※ 개발 서버는 테스트용이므로 필요할 때만 켜고 끌 것.

## 화면 흐름 (src/app/page.tsx가 전체를 관리)
```
intro → profile(기본정보) → stance(절대 조건) → survey(96문항)
     → strictness(매칭 기준) → signin(메일로 저장) → me(내 프로필) ⇄ matches(결과 미리보기)
```
설문은 답할 때마다 브라우저에 임시 저장된다(storage.ts).
`.env.local`에 Supabase 연결 정보가 있으면 마지막에 계정 저장 화면이 뜨고,
없으면 그 화면을 건너뛰고 예전처럼 브라우저 저장만 쓴다.

## 설계 결정 (사용자 피드백 반영, 최신)

### 기본 원칙 — 기본 정보는 객관적 사실만
나이·키·사는 곳·성별·찾는 성별처럼 **객관적인 것만** 기본 정보에서 받는다.
흡연·음주·종교·결혼 의향·자녀 계획·정치 성향은 **본인에게 직접 묻지 않고
설문 응답으로 우리가 판단한다** (src/lib/facts.ts).
특히 정치 성향은 "본인이 진보인가 보수인가"를 묻지 않고 실제 이슈에 대한
태도를 물어 계산한다. 자기평가는 실제 태도와 자주 어긋나기 때문이다.

### 화면·입력
- **나이는 휠로 고른다** — 직접 입력하면 오타가 난다. 만 19세 이상 연도만 보여준다
- **나이·키 범위는 드래그 슬라이더** — 볼륨 조절하듯 양쪽 손잡이를 잡아 끈다.
  나이는 "상관없음"을 따로 켤 수 있다
- **다음 버튼은 항상 눌린다** — 비활성으로 막으면 뭐가 문제인지 알 수 없다.
  누르면 빠진 항목을 빨갛게 표시하고 그 위치로 스크롤한다
- **설문은 한 화면에 5문항씩** — 하나씩 넘기면 30번 넘겨야 해서 번거롭다
- **사는 곳은 시/군/구까지** (서울 마포구)
- **"어디서 만날까"가 아니라 "얼마나 멀리 갈 수 있나"를 묻는다** — 시/도로 고르게 하면
  경기도가 너무 크고(파주-평택 126분), 250개 시군구를 다 고르게 하면 입력이 고역이다.
  사는 곳은 이미 받으니 두 사람 사이 예상 이동 시간을 계산한다 (src/lib/zones.ts).
  권역 19개(경기는 남/북, 강원은 영서/영동으로 분리) 좌표로 직선거리를 재고 시간으로 환산한다.
  지도 API는 매칭이 사람 수의 제곱이라 호출량을 감당할 수 없어 쓰지 않는다
- **거리는 두 사람의 허용 시간을 더해서 본다** — 사람들은 보통 중간에서 만난다.
  각자 답한 시간은 "상대 집까지"가 아니라 "내가 편도로 움직일 수 있는 시간"이다.
  강북(90분) + 수원(60분) = 150분이면 집 사이가 88분이라도 만날 수 있다.
  둘 다 30분씩만 가능하면 같은 88분 거리도 걸러진다
- **선택 입력에는 "입력 안 함" 버튼을 둔다** — "넘어가셔도 됩니다"라고 써두면
  그냥 지나쳐도 되는지 확신이 안 선다. 누를 수 있는 선택지로 만들어야 한다
- **설문 완료 안내는 팝업으로** 띄우고 바로 프로필 화면을 보여준다

### 절대 조건 (최대 3개, 0개도 가능)
- **짧은 단어만 나열한다** — 문장으로 길게 쓰면 한눈에 안 들어온다.
  "흡연"을 골랐다는 것 자체가 그 사람에게 흡연이 가장 중요하다는 뜻이다
- **규칙은 하나 — 고른 항목은 상대가 나와 같아야 한다.**
  그래서 값을 따로 묻지 않는다. 내 값은 설문에서 알아낸다
- 프로필에는 구체적으로 표시한다 ("결혼관" → "비혼주의")
- 정말 치명적인 것만 넣는다. "과음하지 않는 분" 같은 애매한 건 뺐다
- 항목 13개: 흡연 · 음주 · 종교 · 결혼관 · 자녀 계획 · 정치 성향 · 운동 · 반려동물
  · 취미 · 소비 성향 · 생활 리듬 · 연락 빈도 · 학력
- 취미는 "같아야"가 아니라 "관심사가 2개 이상 겹쳐야",
  소비·리듬·연락은 "관련 문항의 응답 평균이 비슷해야"로 판정한다
- **거리·나이·키는 절대 조건과 별개로 항상 적용** — 기본 정보에서 범위로 받는다.
  키를 절대 조건 안에 두면 "내 키인지 상대 키인지" 헷갈린다는 피드백이 있었다.
  기본 정보에서 "내 키"(휠)와 "원하는 상대의 키"(범위 슬라이더)를 나란히 받는다

### 매칭 기준 (설문 직후 사용자가 고름)
궁합 점수(100점 만점) 기준으로 세 가지 중 하나. 프로필에서 언제든 바꾼다.
- 아주 잘 맞는 분만 = 상위 3% · 어느 정도(추천) = 상위 10% · 조금 달라도 = 상위 25%
- **점수는 백분위로 정의한다.** 원점수를 직선으로 늘리면 문항을 더하거나 뺄 때마다
  같은 80점이 다른 뜻이 된다. 그래서 matching.ts의 RAW_ANCHORS / SCORE_ANCHORS로
  분포상의 위치를 점수에 대응시킨다. 80점은 항상 "상위 10%"를 뜻한다
- RAW_ANCHORS는 지금 가짜 사용자 600명(약 10,000쌍)에서 뽑은 값이다.
  **실사용자가 쌓이면 같은 방법으로 다시 뽑아 이 배열만 갈아끼운다.**
  SCORE_ANCHORS(무엇을 몇 점이라 부를지)는 제품 결정이라 그대로 둔다
- 기준을 넘는 사람이 없으면 빈손으로 두지 않고 기준을 넓혀보라고 안내한다
- **기준 점수는 화면에 보여주지 않는다** — 사용자에게 필요한 건 "얼마나 자주, 얼마나 잘 맞는
  사람을 볼 것인가"이지 내부 계산 기준이 아니다. 숫자를 보여주면 그 뜻부터 설명해야 한다
- **설문에서 파악한 값(흡연·정치 성향 등)도 사용자에게 보여주지 않는다** — 내부 판정용이다

### 계정과 저장 (Supabase)
- **계정은 설문을 다 마친 뒤에 만들게 한다.** 시작하자마자 가입을 시키면
  아무것도 못 해본 사람에게 개인정보를 내놓으라는 말이 된다. 끝까지 해보게 한 뒤
  "이 결과를 잃어버리지 않으려면"이라고 묻는 편이 자연스럽고 이탈도 적다
- **비밀번호를 만들지 않는다.** 메일로 온 링크를 눌러 로그인한다.
  외울 것이 없고, 우리가 비밀번호를 보관하지 않아 새어나갈 것도 없다.
  카카오·구글은 각 회사 앱 등록·심사가 필요해서(카카오는 사업자 정보 요구) 실서비스 준비 단계에 붙인다
- **연결 정보가 없어도 앱은 그대로 돌아간다.** `supabaseReady`가 false면
  저장 화면을 아예 건너뛴다. 시제품을 계속 손볼 수 있어야 하기 때문
- **설문 응답은 칼럼으로 쪼개지 않고 jsonb 한 덩어리로 넣는다.** 문항이 계속 바뀌는데
  칼럼을 쓰면 문항을 고칠 때마다 표 구조를 바꿔야 한다
- **접근 권한(RLS)을 반드시 켠다.** Supabase는 기본이 "누구나 읽기 가능"이다.
  특히 `answers`는 본인 외 누구에게도 열지 않는다 — 매칭 계산은 서버에서만 한다
- 표 구조와 권한은 `supabase/schema.sql`, 연결 절차는 `docs/supabase-setup.md`

### 토큰 — 누가 언제 내는가 (결정)
**먼저 말을 거는 쪽만 낸다. 답장은 무료.**
- **"둘 다 사놓고 매칭되면 하나씩 소진"은 채택하지 않는다.** 사용자가 스스로 짚었듯
  논란의 소지가 크다. 매칭은 우리가 만든 것이지 사용자가 고른 것이 아닌데
  토큰이 본인 의사와 무관하게 빠져나간다. 게다가 **매칭을 많이 만들수록 우리가 돈을 버는**
  구조가 되어 "정확도에는 과금하지 않는다"는 원칙과 정면으로 충돌한다
- **양쪽이 다 내야 대화가 열리는 구조도 채택하지 않는다.** 받는 사람은 아직 상대에게
  관심이 없는 상태인데 돈부터 내라는 말이 된다. "결제 안 하면 온 연락도 못 읽는다"로 느껴진다
- **그래서 돈은 사용자가 스스로 누른 순간에만 나간다** — 먼저 말 걸기, 추가 인연 받기.
  배급·매칭·좋아요는 전부 무료
- **72시간 안에 답장이 없으면 토큰을 돌려준다** (`token_ledger.reason = 'refund_no_reply'`).
  "돈만 날렸다"는 기억이 남으면 재구매가 없다. 이 안전장치가 첫 구매 문턱을 크게 낮춘다
- **좋아요는 무료**로 두고, 서로 눌렀으면 토큰 없이 대화방이 열린다.
  무료로 한 번 성공해봐야 유료를 쓸 근거가 생긴다.
  "누가 나를 좋아하는지 보려면 결제"는 흔하지만 신뢰를 깎는 구조라 쓰지 않는다
- **가입하면 토큰 1개를 준다** (`signup_bonus`). 첫 연락 한 번은 무료 — 사용자가 요청한 항목
- **잔액을 직접 고치지 않고 내역을 쌓아 합계를 낸다** (`token_ledger` + `token_balance` 뷰).
  잘못 빠져나갔을 때 되돌리기 쉽고, 무엇에 썼는지가 남는다

### 매칭·배급
- **배급은 주기 + 예외의 조합** — 기본은 주 1회 정각 배급이되,
  아주 높은 궁합(예: 85점 이상)이 나타나면 주기를 무시하고 즉시 알림.
  같은 두 사람에게 서로를 동시에 배달해 행동 동기를 만든다
- **설문 직후 매칭 결과를 보여주지 않는다** — 프로필 페이지로 보내고
  인연은 알림으로 따로 전달한다
- **과금은 매칭이 필요한 순간에만** — 먼저 연락하기 / 추가 인연 받기 두 곳
- **가짜 사용자는 성향 무리(클러스터) + 잡음으로 만든다** — 완전 무작위로 만들면
  궁합 높은 사람이 애초에 없어서 최고 점수가 40점대에 머문다. 기준 검증이 불가능해진다

### 설문은 한 번에, 대신 임시 저장
- 96문항을 한 흐름으로 끝낸다. 한 화면에 5문항씩 20쪽
- **쪽 수·답변 개수·문항 번호를 보여주지 않는다** — 숫자가 보이면 남은 양이 부담으로 온다.
  진행률은 상단 막대로만 알린다
- 답할 때마다 브라우저에 자동 저장한다 (src/lib/storage.ts)
- 다시 들어오면 "작성하던 설문이 있어요" 팝업으로 이어서 하기를 제안한다
- 1차/2차로 나누는 안은 폐기했다. 사용자가 한 번에 끝내는 쪽을 택했다

### 문항 설계
- **설문은 챕터로 나눠 보여준다** — 22쪽이 내리 이어지면 "언제 끝나지" 싶어 지친다.
  문항은 섞은 채로 두고 지금 어느 대목인지만 이름으로 알린다 (questions.ts의 `chapterOf`).
  생활과 취향 → 어떤 사람인지 → 연애할 때는 → 살아가는 방식 → 믿음과 생각
- **간접적으로 묻되 5점 척도는 유지한다** — 상황형 선택지로 바꿨더니
  보기가 매번 달라져서 그때마다 읽어야 하는 부담이 생겼다.
  문장을 짧게 돌려 쓰되 답하는 방식은 그대로 두는 편이 손이 안 멈춘다
- **직접 묻지 말고 돌려서 묻는다** — 사람들은 직접적인 질문에 솔직하지 못하다.
  "연인과 정치 성향이 달라도 괜찮다" 대신 "정치 얘기가 나오면 화제를 돌리는 편이다",
  "신앙이 삶에서 차지하는 비중이 크다" 대신 "힘든 일이 생기면 기도하거나 마음을 맡기는 곳이 있다".
  **아직 일부만 바꿨다. 나머지 문항도 같은 방식으로 손봐야 한다**
- **페이지는 응답 형식으로 묶고, 영역은 섞는다** (questions.ts의 `PAGES`)
  - 한 페이지는 **형식이 같아야 한다.** 5점 척도를 찍다가 갑자기 취미를 고르는
    화면이 나오면 손이 멈춘다
  - 한 페이지 안에서는 **영역이 섞여야 한다.** 종교만 다섯 개 연달아 나오면
    취조당하는 기분이 든다. 생활 하나, 성향 하나, 연애 하나 식으로 번갈아 꺼낸다
  - 페이지 수가 애매하면 고르게 편다. 11문항을 5씩 자르면 5·5·1이 되는데 4·4·3으로 만든다
- **순서는 문항별 `sensitivity`(1~5)로 정한다** — 가벼운 것부터 무거운 것 순.
  앞쪽에 정치가 나오면 "갑자기 왜 이런 걸 묻지?" 싶어 이탈한다.
  영역 순서가 아니라 문항 단위 민감도라, 영역이 섞이면서도 수위는 완만하게 올라간다
- **관심사는 3개 문항으로 쪼갰다** — 운동·활동 / 문화·예술 / 그 밖에. 한 문항에 70개를
  넣으면 한 화면이 그것만으로 꽉 찬다
- **문항마다 비중이 다르다** (questions.ts의 `WEIGHT`) — 모든 문항이 똑같이
  중요하지는 않다. 관계를 실제로 갈라놓는 것을 크게 잡는다.
  **전체 점수에서 차지하는 비중** (측정해서 맞춘 값):

  | 주제 | 비중 | 문항 수 |
  |---|---|---|
  | 결혼·자녀 | 11.3% | 7 |
  | 종교 | 10.0% | 5 |
  | 애착 | 9.8% | 7 |
  | 정치 | 7.5% | 6 |
  | **음주** | **9.0%** | **5** |
  | 흡연 | 6.0% | 3 |
  | 취향 전체 | 5.0% | 6 |

  **처음에는 음주가 2.1%밖에 안 됐다.** 문항이 하나뿐인데 생활 습관 15%를
  18문항과 나눠 가졌기 때문이다. 취미 3문항(5%)보다 작다는 뜻이라 의도와 정반대였다.
  생활 습관 영역을 25%로 올리고 음주 문항을 5개로 늘려 맞췄다.
  "얼마나 마시나요" 하나로는 같은 "가끔"이 전혀 다른 사람일 수 있어서,
  술자리에서의 행동·다음 날을 대하는 태도를 상황 문항으로 함께 묻는다.
  **이 숫자들은 측정값이 아니라 제품 판단이다.** 실사용자에게서 "어떤 항목이 어긋났을 때
  실제로 헤어지는가"를 관찰하면 그때 근거를 갖고 다시 잡아야 한다
- **한 문항이 한 축만 재는 것은 아니다** — 역할관 문항도 정치 성향에 일부 기여한다.
  `politicsWeight`(0~1)로 기여도를 조절한다. 결혼 전 동거·맞벌이 등이 여기 해당
- **영역별 비중은 문항 수와 무관하게 유지된다** — 가중치를 영역 내 문항 수로 나눈다.
  이렇게 하지 않으면 문항이 많은 영역이 자동으로 더 큰 힘을 갖는다
  (실제로 가치관이 의도한 35%가 아니라 47.5%로 작동하던 버그가 있었다).
  이제 문항 수는 측정의 정밀도만 올린다
- **유형은 묻지 말고 잰다** — MBTI·애착 유형·테토/에겐 같은 라벨을 직접 묻지 않는다.
  자기 진단은 부정확하고 유행어는 정의가 사람마다 다르다.
  재는 것은 행동 문항으로, 보여주는 것은 유형 이름으로 분리한다
- **애착만은 "비슷할수록 좋다"가 아니다 (구현 완료)** — src/lib/attachment.ts.
  불안·회피 두 축을 재고 4유형(안정/불안/회피/혼란)으로 나눈 뒤 궁합표로 계산한다.
  실측: 불안×회피 22점 · 안정×불안 80점 · 불안×불안 64점 · 안정×안정 96점.
  유형만으로 딱 자르면 경계선(0.49 vs 0.51)이 억울해지므로 실제 점수 근접도를 2할 섞는다.
  화면에는 임상 용어를 쓰지 않고 "가까이 붙어 있고 싶은 편"처럼 부드럽게만 보여준다
- **5점 척도가 기본이되, 4~5지선다도 섞는다** — 여행 스타일처럼 정도가 아니라
  종류를 묻는 문항은 선택지가 맞다

### MBTI
**받되 매칭 계산에는 쓰지 않는다.** 프로필에 보여주는 용도로만.
같은 사람이 재검사하면 유형이 자주 바뀌어 근거로 삼기 어렵고,
우리는 이미 설문으로 성향을 더 정확히 재고 있어 이중 측정이 된다.
다만 다들 알고 있어 입력이 1초고 대화 소재가 되므로 프로필 항목으로는 가치가 있다.
성향 유형 카드와는 떨어뜨려 "내 정보"에 둔다 — 나란히 두면 둘이 어긋날 때 신뢰가 흔들린다.

### 성향 유형
Big Five 중 외향성·개방성·성실성 3축 조합으로 8가지.
**이름은 아직 임시다.** (주도형/모험형/다정형/사교형/탐구형/몽상형/원칙형/여유형)
"이런 분과 잘 맞아요"는 근거가 약해서 뺐다.

## 나중에 하기로 한 것 (사용자가 "나중에"라고 말한 항목 — 잊지 말 것)
- **데이트 코스 추천** — 매칭된 두 사람의 취향 데이터로 어디서 무엇을 할지 추천
- **성향 유형 이름·설명 다듬기**
- **연봉 등 절대 조건 추가 여부** — 연봉은 검증 불가라 신중해야 함
- **직업 입력** — 넣을지 말지 미정
- **내 정보 화면 수정** — 표시 항목과 편집 방식 손봐야 함

## 다음 작업
**Supabase 계정 만들고 연결 정보 넣기.** 코드·표 구조·권한은 다 준비됐다.
`docs/supabase-setup.md`의 5단계(계정 → 프로젝트 → SQL 실행 → 키 복사 → 링크 주소 등록)만
사용자가 직접 하면 켜진다. Vercel 환경변수에도 같은 값을 넣고 재배포할 것.

그다음: **좋아요 → 매칭 성사 → 대화** 화면 (표는 이미 있다).
그다음: 배급(주 1회 + 고득점 즉시 알림).
